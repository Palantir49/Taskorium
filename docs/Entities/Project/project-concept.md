# Проект (Project) — доменное понятие

> 💡 **Цель**: Описать, что такое `Project` в предметной области, какие у него свойства, ограничения и как он связан с другими понятиями (Issue, Status, Workspace).

---

## 🔑 Единый язык

| Термин | Определение |
|--------|-------------|
| **Проект** | Коллекция задач, сгруппированных по статусу.|
| **Статус** | Состояние задачи в рамках конкретного проекта. Определяет этап жизненного цикла задачи и влияет на поведение.|

---

## 📦 Атрибуты

| Атрибут | Тип | Обязательный? | Комментарий |
|--------|-----|---------------|-------------|
| `Id` | `Guid` | ✅ | Глобальный идентификатор. **Неизменяем после создания**.|
| `Workspace_id` | `Guid` | ✅ | К какой рабочей области принадлежит. **Неизменяем после создания**. |
| `Name` | `string` | ✅ | Отображаемое название. Может меняться. **Не может быть пустым**|
| `Description` | `string` | ✅ | Описание проекта.|
| `CreatedDate` | `DateTimeOffset` | ✅ | Дата создания проекта **Неизменяем после создания**.|
| `StartDate` | `DateTimeOffset` | ✅ | Дата начала работы проекта. Может меняться.|
| `FinishDate` | `DateTimeOffset` | ✅ | Дата завершения работы проекта. Может меняться.|

---

## ⚖️ Инварианты

**INV-001**: После создания проекта изменение `Workspace_id` **запрещено**.

**Причина**: Является фичей. Можно реализовать на уровне домена, но в остальных случаях опустить. Если будет время - реализовать.

**INV-002**: При создании `Project` должны быть созданы статусы каждого типа *См. открытый вопрос #5*