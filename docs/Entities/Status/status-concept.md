# Статус задачи (Status) — доменное понятие

> 💡 **Цель**: Описать, что такое `Status` в предметной области, какие у него свойства, ограничения и как он связан с другими понятиями (Project, Issue).

---

## 🔑 Единый язык

| Термин          | Определение                                                                                                           |
| --------------- | --------------------------------------------------------------------------------------------------------------------- |
| **Статус**      | Состояние задачи в рамках конкретного проекта. Определяет этап жизненного цикла задачи и влияет на поведение `Issue`. |
| **Тип статуса** | Категория поведения: `Initial`, `InProgress`, `Terminal`. Определяет, как `Issue` реагирует на переход в этот статус. |

---

## 📦 Атрибуты

| Атрибут | Тип | Обязательный? | Комментарий |
|--------|-----|---------------|-------------|
| `Id` | `Guid` | ✅ | Глобальный идентификатор. **Неизменяем после создания**. |
| `ProjectId` | `Guid` | ✅ | К какому проекту принадлежит. **Неизменяем после создания**. |
| `Name` | `string` | ✅ | Отображаемое название («В работе», «На проверке»). Может меняться. |
| `Position` | `int` | ✅ | Порядок вывода. Может меняться. |
| `Type` | `StatusType` | ✅ | `Initial` / `InProgress` / `Terminal`. **Неизменяем после создания**. |

---

## ⚖️ Инварианты

**INV-001**: После создания статуса изменение `ProjectId` и `Type` **запрещено**. *Смотри открытый вопрос #4*

**Причина**:  
- `ProjectId` определяет контекст существования статуса. Его смена нарушает целостность ссылок из задач.  
- `Type` напрямую влияет на поведение `Issue` (например, проставление `StartedAt`/`CompletedAt`). Его изменение может привести к несогласованному состоянию уже существующих задач.

**Пример**:  
- Задача в статусе `"Отменена"` (`Type = Terminal`) → `CompletedAt = 2026-01-01`.  
- Админ меняет `"Отменена".Type = InProgress`.  
- Теперь задача формально «в работе», но `CompletedAt` остаётся → противоречие.

---

## 📌 Особенности проектирования

- **Позиционирование (`Position`)**:  
  Дырки в последовательности разрешены (например, 1, 2, 5). Сортировка в при ответе — по возрастанию `Position`, при равенстве — по `Id`.

- **Тип по умолчанию для новых задач**:  
  В `Project` рекомендуется хранить `DefaultInitialStatusId`, чтобы однозначно определять стартовый статус.

- **Отображение в UI**:  
  Статусы отображаются в порядке `Position`, сгруппированы по типу (например: все `Initial` → все `InProgress` → все `Terminal`).