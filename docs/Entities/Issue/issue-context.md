# Задача (Issue) — доменное понятие

> 💡 **Цель**: Описать, что такое `Issue` в предметной области, какие у него свойства, ограничения и как он связан с другими понятиями (Project, Status, Type).

---

## 🔑 Единый язык

| Термин | Определение |
|--------|-------------|
| **Проект** | Коллекция задач, сгруппированных по статусу.|
| **Набор статусов проекта** | Уникальный для каждого `Project` список статусов.|

---

## 📦 Атрибуты

| Атрибут | Тип | Обязательный? | Комментарий |
|--------|-----|---------------|-------------|
| `Id` | `Guid` | ✅ | Глобальный идентификатор. **Неизменяем после создания**|
| `Name` | `string` | ✅ | Отображаемое название. Может меняться. **Не может быть пустым**|
| `Description` | `string` | ✅ | Описание задачи. Может меняться.|
| `ProjectId` | `Guid` | ✅ | К какому проекту принадлежит. Может меняться. **Не может быть пустым**|
| `TaskTypeId` | `Guid` | ✅ | Какой имеет тип задачи. Может меняться. **Не может быть пустым**|
| `TaskStatusId` | `Guid` | ✅ | Статус задачи. Может меняться. **Не может быть пустым**|
| `ReporterId` | `Guid` | ✅ | Пользователь, ответственный за задачу / исполнитель. Может меняться.|
| `CreatedDate` | `DateTimeOffset` | ✅ | Дата создания задачи. **Неизменяем после создания**|
| `UpdatedDate` | `DateTimeOffset` | ✅ | Дата последнего изменения задачи. Может меняться. |
| `ResolvedDate` | `DateTimeOffset` | ✅ | Дата выполнения (срок). Может меняться. |
| `DueDate` | `DateTimeOffset` | ✅ | Дата завершения задачи. Может меняться. |

---

## ⚖️ Инварианты

**INV-001**: После создания проекта изменение `ProjectId` **запрещено**.

**Причина**: Является фичей. Можно реализовать на уровне домена, но в остальных случаях опустить. Если будет время - реализовать.

**INV-002**: При создании `Project` должны быть созданы статусы каждого типа *См. открытый вопрос #5*