# Жизненный цикл задачи: типы статусов и их влияние на поведение Issue

> 💡 **Цель**: Описать, как `Issue` ведёт себя при переходах между статусами трёх типов: **начальный**, **в работе**, **завершающий**.

---

## 🔑 Единый язык

| Термин                                      | Определение                                                                                            |
| ------------------------------------------- | ------------------------------------------------------------------------------------------------------ |
| **Начальный статус (`Initial`)**            | Статус по умолчанию при создании задачи. Задача ещё не взята в работу. Пример: `Открыта`, `Назначена`. |
| **Статус «в работе» (`InProgress`)**        | Задача взята в работу. Примеры: `В работе`, `На проверке`.                                             |
| **Завершающий успешный статус (`Success`)** | Задача закрыта. Примеры: `Выполнена`                                                                   |
| **Статус отклонения ( Rejected )**          | Задача отклонена. Примеры:  `Отменена`, `В архив`.                                                     |
| **Дата начала (`StartDate`)**               | Время первого перехода в статус «в работе». Устанавливается один раз.                                  |
| **Дата завершения (`DueDate`)**             | Время первого перехода в завершающий статус. Устанавливается один раз.                                 |

> [!Warning] Статус может принадлежать **только одному типу** одновременно.

---

## ⚖️ Инварианты

**INV-001**: При создании задачи выставляется **начальный статус**, `StartDate = null` и `DueDate = null`

**INV-002**: При переходе из **начального статуса** → в **статус "в работе"** **и** если `StartDate == null` → `StartDate = now`.

**INV-003**: При переходе в **завершающий статус** **и** если `DueDate == null` → `DueDate = now`.

**INV-004**: Нельзя перейти в завершающий статус, если `StartDate == null`. *См. открытый вопрос #1*

**INV-005**: `StartDate` и `DueDate` **не могут быть изменены** после установки. *См. открытый вопрос #2*

---

## 🧪 Поведение: сценарии (Given-When-Then)

### Сценарий 1: Создание задачи → начальный статус *Смотри открытый вопрос #3*
- **Дано**: Создаётся новая задача  
- **Когда**: `Issue.Create(...)`
- **Тогда**: 

### Сценарий 2: Переход в «в работу» → проставляется StartedAt
- **Дано**:  
  `Status = "Открыта"` (начальный),  
  `StartedAt = null`,  
  `CompletedAt = null`
- **Когда**: `ChangeStatus(statusInProgress)` где `statusInProgress.Type == StatusType.InProgress`
- **Тогда**:  
  `StartedAt = now`,  
  `CompletedAt = null`

### Сценарий 3: Переход в завершающий статус → проставляется CompletedAt
- **Дано**:  
  `Status = "В работе"`,  
  `StartedAt = 2026-01-01 09:00:00`,  
  `CompletedAt = null`
- **Когда**: `ChangeStatus(statusDone)` где `statusDone.Type == StatusType.Terminal`
- **Тогда**:  
  `CompletedAt = now`,  
  `StartedAt` не меняется

### Сценарий 4: Откат статустов → сброс дат
- **Дано**:  
  `Status = "Выполнена"`,  
  `CompletedAt = …`, `StartedAt = …`
- **Когда**: `Reopen()`  
- **Тогда**:  
  `Status = initialStatus` (или `In Progress` — уточнить),  
  `CompletedAt = null`,  
  `StartedAt` **не сбрасывается** (если не требуется иное)